openapi: 3.0.1
info:
  title: ForEach_API
  description: API Definition of ForEach_API
  version: 1.0.0
servers:
- url: http://localhost:8290/foreach
paths:
  /:
    post:
      responses:
        default:
          description: Default response
